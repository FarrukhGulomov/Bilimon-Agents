{
  "name": "bilimon-agents",
  "version": "0.1.0",
  "description": "Multi-agent pipeline that discovers, researches, enriches, localizes, validates, and exports learning-institution data for BilimOn (Uzbekistan education marketplace).",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=18"
  },
  "bin": {
    "pipeline": "./dist/cli.js"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "pipeline": "tsx src/cli.ts",
    "test": "tsx test/run-all.ts"
  },
  "dependencies": {
    "openai": "^7.8.0",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "@types/node": "^20.14.9",
    "tsx": "^4.16.2",
    "typescript": "^5.5.3"
  }
}
